/**
 * If a Material UI component is reused often, 
 * it is a good idea to place it here.
 * This will allow the IDE to autocomplete with
 * the same component.
 */

export {Popper} from '@mui/material/Popper';