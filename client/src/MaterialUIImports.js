/**
 * If a Material UI component is reused often, 
 * it is a good idea to place it here.
 * This will allow the IDE to autocomplete with
 * the same component.
 * 
 * This is also useful when using various Material
 * UI components at the same time. It will condense
 * all of the imports into a single line.
 */

export {default as Popper} from '@mui/material/Popper';
export {default as Card} from '@mui/material/Card';
